#import <Foundation/Foundation.h>
#import "calc.h"

int main()
{
	operation* a=[[operation alloc]init];		//creation of object and initialisation
	[a setNum1:5];				//setting the values for the variables.
	[a setNum2:2];
	[a add];
	[a sub];
	[a mul];
	[a div];
	[a release];
	return 0;
}
